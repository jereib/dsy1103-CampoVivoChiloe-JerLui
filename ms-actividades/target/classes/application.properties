spring.application.name=ms-actividades

# Puerto donde correr� este microservicio
server.port=8084

# Configuraci�n de base de datos H2 en memoria
spring.datasource.url=jdbc:h2:mem:actividadesdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
# Desactivamos la creaci�n autom�tica de tablas con Hibernate
# porque queremos usar migraciones con Flyway
spring.jpa.hibernate.ddl-auto=update
# Permite abrir la consola web de H2
spring.h2.console.enabled=true
# Activa Flyway para ejecutar scripts SQL al iniciar
spring.flyway.enabled=true
